using System;
using UnityEngine;
//using uPLibrary.Networking.M2Mqtt;
//using uPLibrary.Networking.M2Mqtt.Messages;
public class MqttManager
{
    //private MqttClient client;
    //private string brokerIpAddress = "your-broker-ip-address"; // MQTT 브로커의 IP 주소
    //private string topic = "your-topic"; // 구독할 토픽
    //public MqttManager()

    //        client = new MqttClient(brokerIpAddress);
    //client.MqttMsgPublishReceived += Client_MqttMsgPublishReceived;

    //    string clientId = "UnityClient" + Random.Range(0, 1000);
    //client.Connect(clientId);
    
    //    client.Subscribe(new string[] { topic
    //}, new byte[] { MqttMsgBase.QOS_LEVEL_AT_LEAST_ONCE });
	
}

